server:
  port: 80

spring:
  application:
    name: cloud-order-service # Eureka服务注册时用到

eureka:
  client:
    register-with-eureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:7001/eureka