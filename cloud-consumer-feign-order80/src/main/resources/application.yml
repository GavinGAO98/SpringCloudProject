server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/, http://eureka7002.com:7002/eureka/


logging:
  level:
    # feign日志监控某个接口的级别
    com.gavin.cloud.service.PaymetFeignService: debug
